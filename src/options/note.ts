import { type YArgsOptionDefinition } from '../types'

const NOTE_OPTION: YArgsOptionDefinition = [
  'note',
  {
    describe: 'Attach a note to the affected entry',
    type: 'string'
  }
]

export default NOTE_OPTION
