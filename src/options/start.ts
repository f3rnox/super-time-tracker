import { type YArgsOptionDefinition } from '../types'

const START_OPTION: YArgsOptionDefinition = [
  'start',
  {
    describe: 'New start date for the specified entry',
    type: 'string'
  }
]

export default START_OPTION
